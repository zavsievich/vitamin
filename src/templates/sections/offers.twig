{% if not args %}
    {% set args = {
        headingTitle : 'Too much copy supplied ',
        headingSubTitle : 'Needs to be 2 lines max',
        posterInfoTitle : 'Skin glow',
        posterInfoCost : '£12.00',
        posterInfoDescr : 'Natural blend of vitamins and minerals, specifically designed to improve the innate firmness and health of your skin.',
    } %}
{% endif %}
<section class="s-offers">
    <div class="s-offers-wrap">
        <div class="s-offers-heading">
            <h2 class="s-offers-heading__title">{{ args.headingTitle }}</h2>
            <p class="s-offers-heading__sub-title">{{ args.headingSubTitle }}</p>
        </div>
        <div class="s-offers-poster container">
            <div class="s-offers-poster__product">
                <img src="images/cream.png" alt="cream" class="s-offers-poster__product-img">
            </div>
            <div class="s-offers-poster__info">
                <h3 class="s-offers-poster__info-title">{{ args.posterInfoTitle }}</h3>
                <p class="s-offers-poster__info-cost">{{ args.posterInfoCost }}</p>
                <p class="s-offers-poster__info-description">{{ args.posterInfoDescr }}</p>
                {% include "../components/button.twig" with {
                    content : 'View detalis',
                } %}
            </div>
        </div>
    </div>
</section>